import React from 'react';

export function addIconsToLabel(label, className) {
  // Icon support can be added here as RCommon-specific icons become available.
  // For now, return the label without an icon.
  return <div>{label}</div>;
}
