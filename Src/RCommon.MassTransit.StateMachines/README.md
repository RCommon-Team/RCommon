# RCommon.MassTransit.StateMachines

Lightweight dictionary-based state machine adapter for the RCommon framework. Implements `IStateMachine<TState, TTrigger>` and related interfaces using enum-based states and triggers.
